spring:
  jpa:
    output:
      ansi:
        enabled: always
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:development}
  logging:
    level:
      root: DEBUG
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui